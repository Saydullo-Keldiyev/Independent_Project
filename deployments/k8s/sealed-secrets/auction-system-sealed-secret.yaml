# This file is generated by kubeseal — safe to commit to Git.
# The encrypted values below are ONLY decryptable by the cluster's
# Sealed Secrets controller. Regenerate with:
#   kubeseal --format yaml < secret.yaml > this-file.yaml
#
# ⚠️  Replace the encryptedData values with your actual sealed secrets.
#     The values below are PLACEHOLDERS — they will not work.

apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: auction-system-secrets
  namespace: auction-system
  annotations:
    sealedsecrets.bitnami.com/cluster-wide: "false"
spec:
  encryptedData:
    # Run: kubeseal to generate real encrypted values
    DB_URL:            AgBY...PLACEHOLDER...==
    JWT_SECRET:        AgCX...PLACEHOLDER...==
    REDIS_PASSWORD:    AgDZ...PLACEHOLDER...==
    POSTGRES_PASSWORD: AgEW...PLACEHOLDER...==
  template:
    metadata:
      name: auction-system-secrets
      namespace: auction-system
    type: Opaque
