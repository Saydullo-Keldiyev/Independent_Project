apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: bid-service-pdb
  namespace: auction-system
spec:
  # At least 2 pods must stay up during node drain / cluster maintenance
  minAvailable: 2
  selector:
    matchLabels:
      app: bid-service
