apiVersion: v1
kind: Service
metadata:
  name: payment-service
  namespace: auction-system
  labels:
    app: payment-service
spec:
  type: ClusterIP
  selector:
    app: payment-service
  ports:
    - name: http
      port: 80
      targetPort: 8085
      protocol: TCP
